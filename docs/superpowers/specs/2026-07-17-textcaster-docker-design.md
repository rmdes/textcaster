# Textcaster — Docker Compose (dev + self-host) design

Date: 2026-07-17
Status: design approved (brainstorm); spec pending review
Author: Ricardo (rmdes) with Claude Code
Reference: `/home/rmdes/feedland-docker` (Caddy auto-HTTPS, Mailpit basic-auth
at `/mail`, `.env.example` with `${VAR:?}` guards, healthchecks + memory
limits + `restart: unless-stopped`, `scripts/generate-env.sh`).

## Goal

Two Compose files in THIS monorepo: a **dev** stack (`docker compose up`,
full live-reload, no more manual `docker run` for Mailpit) and a **prod**
stack for VPS self-hosters (built images, Caddy HTTPS, Mailpit protected,
WebSub/rssCloud federation reachable). Then rewrite `README.md`.

## Topology recap

- **core** — Hono/Node :8787, SQLite, better-auth. Never browser-facing:
  the browser reaches its PUBLIC surface only through Caddy (prod) or web's
  proxies (dev); its INTERNAL surface is web→core server-side only.
- **web** — SvelteKit. Dev: `vite dev`. Prod: `adapter-node` Node server.
  Finds core via `CORE_API_URL` (default `http://localhost:8787`).
- **mailpit** — mail catcher; UI shows verify/magic links, so it is
  PROTECTED in prod, localhost-only in dev.
- No separate DB (SQLite is the lean-footprint choice).

## The one code change: adapter-node

`web/vite.config.ts` currently inlines `@sveltejs/adapter-auto` (produces
nothing runnable in a plain container). Swap to `@sveltejs/adapter-node`
(new dep, user-approved). Dev is unaffected (`vite dev` ignores the
adapter); only `npm run build -w web` uses it, emitting `web/build/index.js`
(a Node server honoring `PORT` and `ORIGIN`). Plan verifies the build runs
and all existing suites stay green.

## Files

```
compose.yaml              # dev
compose.prod.yaml         # prod
Caddyfile                 # prod reverse proxy + Mailpit basic-auth
.env.example              # documented; ${VAR:?} on required
.dockerignore
docker/Dockerfile.core    # core runtime image
docker/Dockerfile.web     # web multi-stage: build (adapter-node) → runtime
scripts/generate-env.sh   # secrets + bcrypt hash → .env
README.md                 # rewritten
```

## Base image + native module

`node:22-bookworm-slim` (Debian/glibc). `better-sqlite3` ships glibc
prebuilds → no compile on this base (alpine/musl would force a build; avoid).
Builder stages run `npm ci` at the repo root (workspaces hoist to root
`node_modules`).

## Dev — `compose.yaml` (full stack, live reload)

Services: **mailpit**, **core**, **web**.

- **core**: image `node:22-bookworm-slim` (no custom Dockerfile needed for
  dev — command-driven). Whole repo bind-mounted at `/app`; a NAMED VOLUME
  `node_modules:/app/node_modules` shadows the host's so the container's
  Linux-built `better-sqlite3` wins. One-time `npm ci` runs via the command
  (`sh -c "npm ci && npm run dev -w core"`), or a lightweight init. Ports
  `8787:8787`. Env (dev): `TEXTCASTER_AUTH_SECRET=dev-secret-...`,
  `TEXTCASTER_TOKEN=dev-token`, `TEXTCASTER_SMTP_URL=smtp://mailpit:1025`,
  `TEXTCASTER_MAIL_FROM=textcaster@localhost`, `TEXTCASTER_DB=/app/data/dev.db`,
  `TEXTCASTER_WEB_ORIGIN=http://localhost:5173`. **Push OFF**: no
  `TEXTCASTER_PUBLIC_URL`, `TEXTCASTER_WEBSUB`/`RSSCLOUD` unset (default off).
- **web**: same repo mount + `node_modules` volume, command
  `sh -c "npm ci && npm run dev -w web -- --host 0.0.0.0"` (--host so the
  container port is reachable). Ports `5173:5173`. Env
  `CORE_API_URL=http://core:8787`. Hot-reload via the bind mount.
- **mailpit**: `axllent/mailpit`, UI `8025:8025` (unprotected — dev is
  localhost). SMTP reachable in-network as `mailpit:1025`.
- Single shared network (default). No Caddy in dev.
- `.env` for dev is generated by `generate-env.sh --dev` (or copied from
  `.env.example`) — dev secrets can be static/weak, that's fine.

Result: `docker compose up` → core, web, Mailpit; edit files on the host,
they hot-reload in the containers; register a user and the email lands in
Mailpit at :8025 (this IS the pending Task-4 loop, now one command).

## Prod — `compose.prod.yaml` (VPS self-host, federation-ready)

Services: **caddy**, **web**, **core**, **mailpit**.

- **caddy** (`caddy:2-alpine`, ports 80/443): auto-HTTPS for
  `{$TEXTCASTER_DOMAIN}`. Routing (see Caddyfile below) sends core's PUBLIC
  paths to `core:8787`, `/mail*` to Mailpit behind basic-auth, everything
  else to `web:3000`.
- **web**: built from `docker/Dockerfile.web`; runs
  `node build/index.js`, `PORT=3000`, `ORIGIN=https://${TEXTCASTER_DOMAIN}`
  (adapter-node CSRF), `CORE_API_URL=http://core:8787`. Backend + frontend
  networks. Not directly published (Caddy fronts it).
- **core**: built from `docker/Dockerfile.core`; **no published ports**
  (backend network only). SQLite on named volume `core-data:/data`,
  `TEXTCASTER_DB=/data/textcaster.db`. **Push ON** (prod federation, per
  user): `TEXTCASTER_PUBLIC_URL=https://${TEXTCASTER_DOMAIN}`,
  `TEXTCASTER_WEB_ORIGIN=https://${TEXTCASTER_DOMAIN}`,
  `TEXTCASTER_WEBSUB=self` (self-hosted hub; or an external hub URL),
  `TEXTCASTER_RSSCLOUD=on`, `TEXTCASTER_PUSH_IN=on`,
  `TEXTCASTER_AUTH_SECRET`/`TEXTCASTER_TOKEN` (generated),
  `TEXTCASTER_SMTP_URL` (Mailpit default; real SMTP for delivery),
  `TEXTCASTER_MAIL_FROM`.
- **mailpit**: `MP_WEBROOT=/mail`; frontend+backend networks; no published
  ports (Caddy fronts `/mail`). Default mail sink; `.env.example` documents
  pointing `TEXTCASTER_SMTP_URL` at real SMTP for actual delivery (Mailpit
  catches but never delivers — the honest caveat).
- Networks: `frontend` (caddy, web, mailpit), `backend` (web, core, mailpit).
  core ONLY on backend. Healthchecks (core `/health`, web root, mailpit),
  memory limits, `restart: unless-stopped` on all.

### Caddy routing (the public/internal split)

Core's PUBLIC surface (reachable so federation + feeds + auth work) →
`core:8787`; everything else → `web:3000`. Precise allowlist (from core's
routes):

```
{$TEXTCASTER_DOMAIN} {
	# Mailpit UI — MUST be protected (shows verify/magic-link emails)
	handle /mail* {
		basic_auth { {$MAILPIT_USER} {$MAILPIT_PASSWORD_HASH} }
		reverse_proxy mailpit:8025
	}

	# core PUBLIC surface: auth, feeds, federation push-callbacks
	@core {
		path /api/auth/*
		path /users/rss.xml
		path_regexp /users/[^/]+/feed\.(xml|json)$
		path_regexp /users/[^/]+/following\.opml$
		path_regexp /post/[^/]+/comments\.xml$
		path /websub/callback/*
		path /rsscloud/notify /rsscloud/pleaseNotify
		path /hub
	}
	handle @core { reverse_proxy core:8787 }

	# everything else → the SvelteKit app
	handle { reverse_proxy web:3000 }
}
```

INTERNAL (never matched above, stay web→core server-side): `/posts`,
`/me*`, `/timeline`, `/timeline/stream`, `POST /users`,
`/users/:handle/follows`, `/post/:id/thread`, `/peers`, `/health`. `/hub` is
routed only meaningfully when `TEXTCASTER_WEBSUB=self`; harmless otherwise.

## Secrets — `scripts/generate-env.sh`

Generates and writes `.env`: `TEXTCASTER_AUTH_SECRET` and
`TEXTCASTER_TOKEN` (`openssl rand -hex 32`), `MAILPIT_PASSWORD` +
`MAILPIT_PASSWORD_HASH` (via `docker run --rm caddy:2-alpine caddy
hash-password --plaintext …`, so no host bcrypt tool needed). `--dev` flag
writes a dev-flavored `.env` (weak secrets, push off, no domain). Required
prod vars use `${VAR:?message}` in the compose so a missing one fails fast.

## README rewrite

- What Textcaster IS (feeds-native social timeline; RSS-native following/
  threading/federation; Textcasting + Dave-Winer/rss.chat lineage).
- Dev quick-start: `cp .env.example .env` (or `./scripts/generate-env.sh
  --dev`) → `docker compose up` → app at :5173, Mailpit at :8025.
- Self-host guide: point DNS at the VPS → `./scripts/generate-env.sh`
  (fills domain, secrets, Mailpit hash) → `docker compose -f
  compose.prod.yaml up -d`; Caddy issues HTTPS automatically; Mailpit is at
  `/mail` (basic-auth); note real-SMTP for delivery; federation (WebSub/
  rssCloud) is on by default in prod.
- Architecture blurb + links to `docs/superpowers/` specs and RUNNING.md.

## What does NOT change

- Application code, except the one `adapter-auto`→`adapter-node` swap in
  `web/vite.config.ts` (+ dep). No route, auth, or feed logic changes.
- Existing `npm test` (core + web) must stay green.

## Verification (infra — documented smoke checklist, not unit tests)

- Dev: `docker compose up` builds; core `/health` ok; web loads at :5173;
  web→core proxy works (timeline renders); register → email in Mailpit :8025.
- Prod (against a test domain or with a hosts entry + self-signed staging):
  `docker compose -f compose.prod.yaml up -d --build` comes healthy; the
  browser reaches the app over HTTPS; `/mail` prompts basic-auth; a feed URL
  `https://domain/users/rss.xml` returns RSS (proves the core route); a
  WebSub subscribe to `https://domain/users/<h>/feed.xml` reaches core
  (proves federation path). `npm run build -w web` with adapter-node
  succeeds.
- Existing suites: `npm test -w core` and `npm test -w web` green after the
  adapter swap.

## Sequencing

1. adapter-node swap + dep; verify build + suites.
2. Dockerfiles (core, web) + `.dockerignore`.
3. Dev `compose.yaml` + dev `.env` path; smoke it.
4. Prod `compose.prod.yaml` + `Caddyfile` + `.env.example` + secrets script.
5. README rewrite.
